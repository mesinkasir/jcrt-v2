name: Sync Assets To jcrt-files

# DEPRECATED: All images, PDFs, docs, and citations now live in jcrt-files.
# This workflow is disabled. New assets should be added directly to jcrt-files.
# Keeping the file to document the migration. Remove entirely once stable.

on:
  workflow_dispatch:
  # No longer auto-triggered on push — jcrt-v2 no longer contains binary assets.

jobs:
  sync:
    runs-on: ubuntu-latest
    if: false  # Disabled — assets live in jcrt-files now
    steps:
      - run: echo "This workflow is disabled. Assets should be added directly to jcrt-files."
